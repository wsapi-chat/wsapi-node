/**
 * Group picture information
 */
export interface GroupPictureInfo {
  /**
   * The unique identifier of the picture.
   */
  pictureId: string;

  /**
   * The URL to access the picture.
   */
  pictureUrl: string;
}
